# Safe local-development defaults. Copy this file to .env before running Expo.
# The API client adds /api to endpoint paths, so keep this value as the origin only.
EXPO_PUBLIC_ENVIRONMENT=development
EXPO_PUBLIC_API_URL=http://localhost:8000
